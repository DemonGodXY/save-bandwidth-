{
  "name": "image-compression-proxy",
  "version": "1.0.0",
  "description": "Image compression proxy service for Vercel deployment",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
